name: coronapoker-qa

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-fast-qa:
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Check out triggering commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Record and verify commit
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          java -version
          mvn --version

      - name: Build current game and run fast QA in one reactor
        run: mvn -B -ntp -f tools/reactor/pom.xml -P qa-fast verify

      - name: Preserve QA reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fast-qa-${{ github.sha }}
          path: |
            tools/qa/target/surefire-reports/
            target/*.jar
          if-no-files-found: error
          retention-days: 14
